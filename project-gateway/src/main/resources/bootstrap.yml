### ----本地配置----
server:
  port: 10000 # 网关端口
spring:
  application:
    name: project-gateway # 服务名称
  cloud:
    nacos:
      server-addr: 192.168.145.101:80 # nacos地址
      config:
        file-extension: yaml # 文件名后缀
      discovery:
        namespace: dev666
        group: DEFAULT_GROUP # m默认可不写